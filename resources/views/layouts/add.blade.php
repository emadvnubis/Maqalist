@yield('header')
  @yield('navbar')
  <div class="container">
    <div class="row">
      <div class="col-sm-12">
        @yield('form')
      </div>
    </div>
  </div>
  @yield('footer_content')
@yield('footer')
